name: CI with UV

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3

    - name: Install basic dependencies
      run: |
        uv add numpy pandas scikit-learn
        echo "✅ Basic dependencies installed"

    - name: Test imports
      run: |
        uv run python -c "import numpy, pandas, sklearn; print('✅ ALL IMPORTS WORK!')"

    - name: Success
      run: echo "🎉 UV CI WORKS!"
