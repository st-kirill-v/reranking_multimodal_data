name: Minimal CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install ONLY numpy (самая простая зависимость)
      run: |
        python -m pip install --upgrade pip
        pip install numpy
        python -c "import numpy; print('✅ NUMPY WORKS!')"

    - name: Success
      run: echo "🎉 MINIMAL CI WORKS!"
