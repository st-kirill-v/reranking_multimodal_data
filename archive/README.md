# Archive

This directory preserves code that is not part of the clean publication pipeline.
Files are moved here instead of being deleted.

- `legacy_scripts/`: diagnostics, audits, old f1-subset experiments, tile experiments, and other
  exploratory scripts that were useful during development but are not required by the current
  reproducible pipeline.
